# Some tip's and tricks regarding vscode

* `Inlay Hints`: enable in settings
* Use the `problems` view
* Enable `clang-tidy` by default

## Debugging

Make use of cmake's targets for debugging
```
${command:cmake.launchTargetPath}
```



